const numeroSorteado = 9;

function Sorteio() {
    let NumeroUsuario = prompt("Digite um número de 0 a 10");

if (NumeroUsuario == 9) {
    prompt("Parábens!!, você acertou o numero sorteado")    
} else {
    prompt ("Você não acertou, tente novamente")
}
}